

docker-compose.yaml

```yaml
    - ./data/fonts/padauk:/usr/share/fonts/truetype/padauk
```


```sh
docker-compose up -d

docker compose exec --user root web bash

apt-get update

apt-get install -y fontconfig

fc-cache -f -v

fc-list | grep Padauk

fc-list | grep OCSone

```

ocsone module ကို create လုပ်ပါ။

invoice ကို ပြင်ရန် account.report_invoice_document inherit လုပ်ပြီး ပြန်ရန် ocsone_account_report_invoice_report.xml ကို အသုံးပြုပါသည်။

uniocsone.py နဲ့ uniocsoneDict.py ကို uniocsone github ကနေ ရယူပါ။



