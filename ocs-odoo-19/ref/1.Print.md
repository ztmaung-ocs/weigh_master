

docker-compose.yaml

```yaml
- WEB_BASE_URL=http://${web_container_name}:${odoo_port}
```

odoo.conf

```conf 
web.base.url = http://${web_container_name}:${odoo_port}
report.url = http://${web_container_name}:${odoo_port}
 
```
